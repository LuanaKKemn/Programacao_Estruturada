#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void cmpRecursiva();

int main(void) {
  char a[30];
  char b[30];

  // ENTRADA
  printf("Digite a 1ª string: ");
  scanf("%s", &a[30]);

  printf("Digite a 2ª string: ");
  scanf("%s", &b[30]);


  // PROCESSAMENTO



  // SAÍDA

  
  return 0;
}

void cmpRecursiva(char a, char b, int i){
  if (strlen(a) != strlen(char b))
  
}
